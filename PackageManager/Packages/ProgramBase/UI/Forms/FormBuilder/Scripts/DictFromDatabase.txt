def create{RecordGroup.Abbreviation}DictFromDatabase(self, record_id):
    qryRecord = session.query({RecordGroup.Table}).filter_by(id=record_id).first()
    self.init_{RecordGroup.Abbreviation}Dict()
    {BeginWidgetList}
    if qryRecord.{RecordGroup.Form.DatabaseValue} is not None: self.{RecordGroup.Title}Dict["{RecordGroup.Form.ItemName}"] = qryRecord.{RecordGroup.Form.DatabaseValue}
    {EndWidgetList}